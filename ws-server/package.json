{
  "name": "ws-server",
  "version": "1.0.0",
  "description": "독립된 Socket.IO 서버",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "build": "tsc",
    "clean": "rm -rf dist",
    "pm2:start": "pm2 start ecosystem.config.cjs",
    "pm2:stop": "pm2 stop ws-server",
    "pm2:restart": "pm2 restart ws-server",
    "pm2:reload": "pm2 reload ws-server",
    "pm2:delete": "pm2 delete ws-server",
    "pm2:logs": "pm2 logs ws-server",
    "pm2:monit": "pm2 monit",
    "pm2:status": "pm2 status"
  },
  "dependencies": {
    "socket.io": "^4.8.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
