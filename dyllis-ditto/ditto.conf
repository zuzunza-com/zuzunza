# Dyllis Ditto Configuration
# 빌드 및 서버 설정 파일

# 현재 활성 빌드 날짜 (YYYY-MM-DD 형식, 10자리)
# 서버가 이 날짜의 compile 디렉토리를 서빙합니다
ACTIVE_BUILD=1769764032

# 서버 설정
SERVER_PORT=7600
SERVER_HOST=0.0.0.0

# 빌드 설정
# 병렬 빌드 워커 수 (0 = CPU 코어 수 자동 감지)
BUILD_WORKERS=0

# 빌드 히스토리 보관 개수 (오래된 빌드 자동 정리)
BUILD_HISTORY_KEEP=5

# 로그 레벨 (debug, info, warn, error)
LOG_LEVEL=info
ACTIVE_STUDIO_BUILD=1769764032
